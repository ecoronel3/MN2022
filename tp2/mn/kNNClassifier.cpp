#include "kNNClassifier.h"

namespace mn
{
    void kNNClassifier::fit(const Eigen::MatrixXd& X, const Eigen::VectorXd& y)
    {

    }

    uint16_t kNNClassifier::predict(const Eigen::VectorXd& x)
    {
        return 0;
    }
}
